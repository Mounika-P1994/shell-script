#!/bin/bash

export course="Devsecops with AWS" 

# this variableis valid until the process will be alive , it will be accessable to subprocess as well
# euser s 

echo "course name is" $course

#if we store some value or some message using  export command in ~./bashrc(home folder), .bashrc is hidden folder that value will alive after closing the session

#source .basrc

